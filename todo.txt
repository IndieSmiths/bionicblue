(A) when user requests to erase all data, make it so both ORG and APP folders are deleted (unless ORG folder is not empty)
(A) on data screen, show message confirming data was copied/erased
(A) remove soft lock
(B) playtest with few close acquaintances
    - write playtesting instructions (don't publish yet)
    - upload rc1 to PyPI
    - test everything manually once more
    - publish play test instructions to GitHub discussions
    - create release post on GitHub (linking to playtesting instructions discussion)
    - invite people and collect play data
(C) release stable version on PyPI, GitHub and itch (GitHub releases and itch must include executables; check options using wine app, etc.)
(D) make web version (won't be included in first release; will likely need new task for a password screen in order not to rely on local storage)
x add support to gamepad and keyboard keys in screens lack it
x add "go back" buttons in links screen and credits screen (so gamepad users can go back);
x store duration of play on play data in human-friendly format (you can obtain seconds with = last_frame / FPS)
x make it so users can provide custom fps for replay
x make it so quitting the game from pause menu or level manager states also save the play data when in record mode
x modernize play logging: make adjustments, add support for gamepad data
x position bionic blue title at the correct position beforehand, on resource loader (so after exiting replay mode it is already at the right spot)
x playlogging: zip logs when possible
x playlogging: minify recorded data
